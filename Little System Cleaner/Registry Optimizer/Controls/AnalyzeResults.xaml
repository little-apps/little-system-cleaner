<UserControl x:Class="Little_System_Cleaner.Registry_Optimizer.Controls.AnalyzeResults"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:DV="clr-namespace:System.Windows.Controls.DataVisualization;assembly=System.Windows.Controls.DataVisualization.Toolkit"
             xmlns:DVC="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=System.Windows.Controls.DataVisualization.Toolkit"
             mc:Ignorable="d" 
             MinHeight="384" MinWidth="579">
    <UserControl.Resources>
        <DV:ResourceDictionaryCollection x:Key="MyPalette">
            <ResourceDictionary>
                <Style x:Key="DataPointStyle" TargetType="Control">
                    <Setter Property="Background" Value="Purple" />
                </Style>
            </ResourceDictionary>
            <ResourceDictionary>
                <Style x:Key="DataPointStyle" TargetType="Control">
                    <Setter Property="Background" Value="DeepPink" />
                </Style>
            </ResourceDictionary>
        </DV:ResourceDictionaryCollection>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="306" />
            <RowDefinition Height="42" />
        </Grid.RowDefinitions>
        <Border CornerRadius="5" Margin="0,0,5,0">
            <Border.Effect>
                <DropShadowEffect Color="#FF474747" />
            </Border.Effect>
            <Border.Background>
                <RadialGradientBrush>
                    <GradientStop Color="White" Offset="0"/>
                    <GradientStop Color="Gray" Offset="1"/>
                </RadialGradientBrush>
            </Border.Background>
            <Grid>
                <Label FontSize="14" Margin="8,0" Foreground="White" Height="28" HorizontalAlignment="Left">Registry Optimizer</Label>
                <Label FontSize="14" Foreground="White" HorizontalAlignment="Right" Width="78">Step 3 of 4</Label>
            </Grid>
        </Border>
        <DVC:Chart Name="mcChart" Foreground="DarkBlue" Title="Analyze Results" LegendTitle="Registry Size" Grid.Row="1" Palette="{StaticResource MyPalette}" Margin="0,6,0,0" BorderBrush="White">
            <DVC:Chart.Series>
                <DVC:PieSeries Title="Registry Size" IndependentValueBinding="{Binding Path=Key}" DependentValueBinding="{Binding Path=Value}">
                </DVC:PieSeries>
            </DVC:Chart.Series>
        </DVC:Chart>
        <StackPanel Grid.Row="4" HorizontalAlignment="Center" Name="stackPanel1" Orientation="Horizontal" Margin="0,5,0,0">
            <Button x:Name="buttonCompact" Click="buttonCompact_Click" Margin="0,0,6,0" Padding="8,0,8,0" IsDefault="True">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Little_System_Cleaner;component/Resources/Repair.png" VerticalAlignment="Center" />
                    <TextBlock VerticalAlignment="Center" FontSize="15">Compact</TextBlock>
                </StackPanel>
            </Button>
            <Button x:Name="buttonCancel" Click="buttonCancel_Click" Content="Cancel" Margin="0,0,0,0" Padding="8,0,8,0" FontSize="15" />
        </StackPanel>
    </Grid>
</UserControl>
